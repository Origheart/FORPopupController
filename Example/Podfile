use_frameworks!

target 'FORPopupController_Example', :exclusive => true do
  pod 'FORPopupController', :path => '../'
end

target 'FORPopupController_Tests', :exclusive => true do
  pod 'FORPopupController', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
